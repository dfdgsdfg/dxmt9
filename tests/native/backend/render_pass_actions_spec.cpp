// R-BACK-15.16 - load/store action policy assertions land in G4.
// Stub keeps the meson target alive; the real cases live in the
// spec at specs/backend/render-pass-actions/design.md section 6.
int main() {
  return 0;
}
