---
type: "Spec Gap"
title: "PE Recorder Gap"
description: "Implementation and evidence gaps against the PE recorder state and transaction contract."
tags: [specs, gap, d3d9, recorder, pe]
---

# PE Recorder Gap

Domain-owned tracker for [requirements.md](requirements.md). The parent rollup
is [d3d9/gap](../gap.md); formal evidence gaps also appear in
[verification/gap](../../verification/gap.md).

| Area | Status | Current evidence | Missing work / acceptance |
|---|---|---|---|
| Closed StateBlock category/storage surface (`R-CORE-REC-1.2`, `5.1.1`) | ✅ authoritative physical inventory + private flat tables | One 26-row Apply physical inventory names four keyed stores, sixteen fixed stores, and all six VS/PS F/I/B constant stores. It generates fixed storage/accessors and typed physical iteration; clear and candidate COM lifetime walk the same visitor, while Prepare and Commit instantiate all 26 rows with dependent compile failures for a newly unbound keyed, fixed, or constant store. The candidate-owned vertex declaration is explicitly distinguished from staged/transferred bindings: its candidate/snapshot AddRef covers synchronous Apply and the D3D9 live binding remains borrowed. `FixedTrackedState` and `FixedStateTable` keep their flat arrays, occupancy words, and counters private behind bounded APIs without changing pinned footprints. Native exact-scope/negative-access pins live in `dxmt9-pe-stateblock-category-spec` and `dxmt9-pe-typed-slot-spec`; PE compilation instantiates Prepare/Commit. | This is compile-time/native/PE physical-store evidence, not a new temporal model; the existing StateBlock TLA inventory remains the serial/reference algebra. Wine Apply/COM instrumentation remains open. |
| Command-chunk variable payload surface (`R-CORE-REC-3.1.1`) | ✅ closed production API + atomic negative pins / ⚠️ Wine evidence | Raw builder byte append/overwrite is private. Sparse POD sections use an exact type→kind registry plus schema-rule bounds; constant sections/records, UP byte sections, Clear rectangles, placeholder tables, and final table overwrite use dedicated adapters. Every adapter rejection atomically rolls back the active record. SetConst/Clear tails bind their already-written fixed header, and final overwrite binds the active draw header plus canonical kind order, element/count/byte schema, alignment, sequential payload range, and embedded constant range. `dxmt9-pe-chunk-record-value-spec` proves ignored failures cannot commit and runs the valid non-draw/sparse producer matrix through the same callsites. | C++ has no general recursive “contains no pointer member” reflection; pointer-freedom is a closed audited registry over ABI structs plus schema validation, not a claim about arbitrary unregistered structs. Wine runtime malformed/allocation injection remains open. |
| Three-domain state/category contract (`R-CORE-REC-1.*`, `2.*`) | ✅ single transaction owner + typed candidate/category routing / ✅ staged Apply + conditional interval lock / ⚠️ Wine evidence | `PeRecorderState` now exclusively owns a closed `PeStateBlockTransactionState`, which contains Recording/inside-End/poison lifecycle, the fixed `StateBlockRecorded` candidate/constants, private occupied Apply masks, and category-qualified staged COM values. Its lifecycle API performs candidate release, End publication/fail-stop, reset recovery, staging discard, and commit transfer without independent device booleans or raw `void*` staging. `StateBlockRecorded` covers keyed render/TSS/sampler/transform, texture, independent stream source (offset/stride/ref) and frequency tables, index, VS/PS/FVF/vdecl, RT/depth, viewport/scissor/material, clip planes, lights/enables, and all six constant kinds. Recording-phase setters route only to that candidate; live/pending shadows, getters, backend state, and capture journaling stay unchanged, with `MultiplyTransform` remaining the documented prior-value exception. Wrapper snapshots thread `ALL`/`VERTEXSTATE`/`PIXELSTATE` dispositions and exact key masks; Begin/End uses `Explicit`; Capture refreshes existing keys only. Apply prevalidates before backend mutation and transfers each staged retain independently; backend failure poisons. End backend/wrapper failure leaves Recording, discards the candidate, and poisons. The shared conditional production guard covers the whole interval and default-pool resource-count mutations observed by Reset; the common append envelope checks poison before capacity/flush work, so `Query::Issue` and every other child writer fail-stop while successful Reset remains reachable. `dxmt9-pe-stateblock-category-spec` covers owner placement, lifecycle, reset/failure, typed null occupancy, commit transfer, and same-identity retention multiplicity, while `dxmt9-pe-diagnostics-spec` pins the Query/common-append source route. | The changed PE conformance case still needs a Wine runtime rerun; real Wine bridge failure injection and exact COM-object instrumentation remain unavailable in the host-only witness. |
| StateBlock poison/reset lifetime and model/code binding (`R-CORE-REC-5.1.3`–`5.1.4`) | ✅ closed owner + generated transition matrix / ⚠️ Wine evidence | `d3d9_pe_stateblock_transition_table.inc` drives production `planPeStateBlockTransition` and generated `PeStateBlockTransitionTable.tla`; generator checks reject stale output and unmapped enum phase/event/action additions. The bounded model covers Begin/End/Capture/Apply/reset/teardown serial state, candidate disposition, capture publication, staged-ref cardinality, and poison recovery, with expected stale-open, no-poison, no-release, and lost-duplicate controls. Native exhaustive phase/event truth-table pins call the production planner; fake-COM tests separately pin typed-null occupancy and exact same-identity retention/release/transfer multiplicity. The full verifier passes at 131 generated / 34 distinct StateBlock states. | This is bounded serial/cardinality evidence, not C++ object-layout, atomic-ordering, or COM-runtime proof. A Wine reset-after-failed-Apply probe and real bridge/allocation fault injection remain open. |
| Implicit FVF declaration transaction (`R-CORE-7.1`) | ✅ native fake backend + PE source binding / ⚠️ Wine failure injection | `createImplicitFvfDeclTransaction` owns backend then wrapper handles through cache publication and converts backend-null, wrapper-null/throw, and cache reject/throw into value failures with exact-once cleanup. `SetFVF` resolves before live shadow/pending mutation and returns the mapped HRESULT; UP overrides and StateBlock Apply preparation use the same noexcept resolver. `dxmt9-pe-fvf-transaction-spec` injects every reachable owner edge, and `dxmt9-pe-diagnostics-spec` pins resolve-before-shadow ordering. | Wine has no allocation/bridge fault injector for these cold failure edges; ordinary FVF management conformance still needs a rerun with the changed PE binaries. |
| Atomic pending-delta consumption (`R-CORE-REC-2.3`, `3.1`) | ✅ for state/constant append settlement | `settleRecorderAppend` is production-used by normal draw/APPLY_STATE, all four oversized typed batches, tail APPLY_STATE, inline constant fold, and standalone constant records. `dxmt9-pe-shadow-native-spec` and `dxmt9-pe-producer-differential-spec` cover failed retry, canonical order, exact represented consumption, unrepresented tail, and exactly-once acceptance. The bounded PE model now carries pending snapshots, durable records, and consumption witnesses as `(qualified key, value, ordinal)` tokens; `NoLostPending` cannot discharge a newer value from an older durable key. | Bridge/seal/capture-journal failures remain in the distinct commit transaction row; the prepare/accept premise remains non-reentrant under recorder ownership. |
| PE/unix bridge ABI skew fingerprint | ✅ codegen-owned dispatch/schema fingerprint | `gen_wine_bridge.py` hashes declaration-order `BridgeOpcode` ordinals, generated argument-record field order, every source POD record declaration, and layout-affecting preprocessor context (packing directives and ABI macro values) supplied by `device_c.h`/the unix schema. `dxmt9-bridge-codegen-abi-spec` proves identical schemas are deterministic and that prototype reordering, pointed-record layout mutation, packing mutation, and array-extent macro mutation change the fingerprint; generated outputs remain Meson-owned. | The source schema is the pointer-width-independent cross-target layout contract; generated native/WoW64 `Args_*`/`Args32_*` records retain separate target-local standard-layout/trivially-copyable static assertions. Do not use compiler-specific `sizeof`/`offsetof` in the shared hash; runtime mixed-binary Wine handshake evidence remains a separate acceptance layer. |
| Seal/commit/retry/discard transaction (`R-CORE-REC-3.2`–`3.4`) | ✅ bounded model/algebra binding; ⚠️ runtime fault evidence | `settleRecorderCommit` is production-used as one chained settlement in seal/bridge/capture, pending Render Tape drain, alias-before-parent ObjectDestroy admission, builder reset, warm-retainer advancement, and explicit discard/device reset. Bridge failure returns before any Accepted→Capture transition and preserves the sealed projection for retry; accepted capture rejection and the explicit capture-skipped disposition still drain and reset once. `PeRecorderCommit.tla` now checks the generated matrix extracted from `settleRecorderCommit`, including the reusable WarmAdvanced→Unsealed success edge and liveness control for the prior stuck-success cycle. | Host failure injection covers the pure bridge/capture phase matrix, but Wine runtime bridge/capture fault injection and wild evidence remain open. |
| Pending Render Tape identity lifetime (`R-CORE-REC-3.3`, `3.4`, `4.3`) | ⚠️ implementation/native truth table present; promotion evidence open | Wrapper-to-pending transfer, alias-before-parent retirement, bounded alias flush/restart, and `testPendingAliasFlushBeforeReplacementSequence` exist. | GT2 r12 and cross-build capture evidence remain open. The PE builder transaction still needs its own bounded refinement or explicit mapping; `RenderTapeIdentitySegments.tla` alone does not prove it. |
| Kind-qualified local/wire identity (`R-CORE-REC-4.*`) | ⚠️ registry/model binding remains open | Wire identity is `(kind, generation, objectId)` and full-batch validate-before-retain is covered by `dxmt9-chunk-record-registry-spec` and `WireObjectRegistry.tla`; builder dedup checks identity/pointer agreement. `CommandChunkBuilder::HandlePresenceTable` and its linear fallback now key local presence by `(kind, wrapperPointer)`, with native collision, rollback, and overflow pins in `dxmt9-pe-chunk-record-value-spec`; buffer hazards and pending Render Tape destruction pass qualified buffer/object identities. Typed child wrappers use `TextureRef`/`BufferRef`/`SurfaceRef`/`ShaderRef`/`DeclarationRef`/`QueryRef` outputs for identity caching while generic base refs remain only at kind-polymorphic recorder/capture seams. The write-only `g_wireObjectCache` is removed. `WireObjectRegistry::RetainFn` is statically `noexcept`; every registry method now fails callback re-entry closed before locking in debug and release, and the native test exercises that callback path without corrupting the outer retain. | Complete the remaining identity/model binding beyond builder local presence and registry callback coverage. Do not require atomic COM refcounts globally; `D3D9StateBlockImpl` is the preserved atomic exception. |
| Hot/cold and disabled observer boundary (`R-CORE-REC-5.*`) | ⚠️ owners and interface cleanup landed; broad codegen/wild evidence open | `PeRecorderState` retains hot protocol/shadow/counter ownership; a nullable heap-owned `PeCaptureState` retains capture correctness state; nullable `PeDiagnosticsState` owns the diagnostic-only accumulators, clocks, cadence atomics, maps, sampler, and child observer. Feature-specific cached gates now keep module-map, thread-sampler, and debug-only owners from constructing unrelated `PeCallScope`/`PeHotStateSetterTimer` objects or reading clocks; the disabled edge remains one nullable-owner branch. Disabled general device debug logging is a call-site cached-bool macro, while enabled output still uses the same logger. The retainer now preallocates its entries and hash index from `CommandChunkBuilderCapacities::handles`, including the >64/default 256-handle working set. Commit clocks/stats, UP counters, append phase clocks, TLS writes, and hot setter timers are nullable-gated. The four diagnostic virtuals are removed from `D3D9PeRecorderFlush`; child/device scopes use a concrete nullable observer, while protocol and Render Tape virtuals and `FlushPeRecorderForChild` key-function placement remain. `dxmt9-pe-diagnostics-spec` proves zero disabled allocation/callback/clock calls, one owner allocation for each individual gate (including Debug logging), enabled call/clock counts, diagnostic-field ownership, refcount policy, and source seams. Representative x64 object inspection confirms the sole device vtable remains in `d3d9_pe_device.cpp.obj`, while `SetRenderState` and `Surface::GetDesc` use nullable branches with direct enabled-only diagnostic calls. The ~6k-line recorder/device header remains a documented decomposition debt; no broad physical move was made in this wave. | Broaden PE instruction/code-size coverage across the remaining representative entry points and add Wine/wild diagnostics-on evidence. The native and representative object pins do not by themselves prove a runtime speedup. |
| Shared predicate/model-code binding (`R-CORE-REC-6.*`) | ✅ generated state/append/commit freshness; ⚠️ runtime promotion | `d3d9_pe_transition_table.inc` remains the canonical state/append table, while `gen_pe_commit_transition_table.py` extracts the commit matrix directly from `settleRecorderCommit`; state-write effects now derive from a closed kind and commit effects from a closed action, with native static assertions rejecting aggregate plan construction and row/effect drift checked against the table. Both freshness checks run before TLC. Native transition tests cover bridge retry preservation, accepted command with capture rejection, exact-once drain/reset, alias-before-parent destruction, warm advancement, and discard conservation. `PeRecorderCommit.tla` carries a stuck-success expected failure, and `PeStateBlockTransaction.tla` carries poison and staged-release expected failures while choosing successful Reset recovery. | Wine runtime bridge/capture injection, wild evidence, and broader promotion remain open. |

## Implementation order

Follow [spec.md §8](spec.md#8-sequential-implementation-dag): establish failure
injection first; clean observers/cache; repair append/commit settlement; then
decompose state/ownership, qualify identity, bind shared predicates, add formal
evidence, and finish with PE/Wine acceptance. These tracks are sequential
because they overlap the recorder hot files and because later proofs depend on
the earlier failure semantics.
