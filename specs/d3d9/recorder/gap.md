---
type: "Spec Gap"
title: "PE Recorder Gap"
description: "Implementation and evidence gaps against the PE recorder state and transaction contract."
tags: [specs, gap, d3d9, recorder, pe]
---

# PE Recorder Gap

Domain-owned tracker for [requirements.md](requirements.md). The parent rollup
is [d3d9/gap](../gap.md); formal evidence gaps also appear in
[verification/gap](../../verification/gap.md).

| Area | Status | Current evidence | Missing work / acceptance |
|---|---|---|---|
| Three-domain state/category contract (`R-CORE-REC-1.*`, `2.*`) | ✅ scoped keyed-state transition / ⚠️ broader category matrix | Production setters for render, TSS, sampler, transform, and all six constant kinds call `planRecorderStateWrite`; `dxmt9-pe-transition-algebra-spec` exhausts the finite fact tuples plus A→B→A, same-value record-only writes, kind qualification, invalid input, LWW, commutation, live-phase pending replacement, and explicit-Set/prior-value composition. `PeHotStateShadow` now contains only live/pending domains; `StateBlockRecorded` is a separate recorder-owned table. The PE conformance source pins `SetTransform(B) -> MultiplyTransform(C)` while recording, and fixed typed state-block sets plus `dxmt9-core-stateblock-restore-spec` cover Capture without enlargement. A direct recording-phase prior-value call fails closed if the Begin-flush no-pending premise is violated; post-backend End wrapper-allocation failure discards its unpublished candidate. | The changed PE conformance case still needs a Wine runtime rerun. Apply remains effect-by-effect through normal setters: a backend failure after an earlier setter succeeds can partially change live/pending/backend state, so `R-CORE-REC-1.3` failure atomicity is still open. Whole-operation Apply/Capture serialization against concurrent device setters under `D3DCREATE_MULTITHREADED` is also open; per-append locking does not prove that child-operation interval. Non-keyed binding/singleton/light categories remain outside the scoped pure setter migration. |
| Atomic pending-delta consumption (`R-CORE-REC-2.3`, `3.1`) | ✅ for state/constant append settlement | `settleRecorderAppend` is production-used by normal draw/APPLY_STATE, all four oversized typed batches, tail APPLY_STATE, inline constant fold, and standalone constant records. `dxmt9-pe-shadow-native-spec` and `dxmt9-pe-producer-differential-spec` cover failed retry, canonical order, exact represented consumption, unrepresented tail, and exactly-once acceptance. | Bridge/seal/capture-journal failures remain in the distinct commit transaction row; the prepare/accept premise remains non-reentrant under recorder ownership. |
| Seal/commit/retry/discard transaction (`R-CORE-REC-3.2`–`3.4`) | ⚠️ partial | `CommandChunkBuilder::seal` is idempotent; bridge failure preserves the sealed builder; successful commit captures before pending-ref drain/reset; `testPendingChunkLifetimeTruthTable` covers logical lifetime outcomes. | Add one shared settlement predicate, exhaustive/TLA transition coverage, bridge/capture failure injection, exact retry-byte/pin assertions, and discard/reset conservation. |
| Pending Render Tape identity lifetime (`R-CORE-REC-3.3`, `3.4`, `4.3`) | ⚠️ implementation/native truth table present; promotion evidence open | Wrapper-to-pending transfer, alias-before-parent retirement, bounded alias flush/restart, and `testPendingAliasFlushBeforeReplacementSequence` exist. | GT2 r12 and cross-build capture evidence remain open. The PE builder transaction still needs its own bounded refinement or explicit mapping; `RenderTapeIdentitySegments.tla` alone does not prove it. |
| Kind-qualified local/wire identity (`R-CORE-REC-4.*`) | ⚠️ registry callback contract landed; broader identity evidence open | Wire identity is `(kind, generation, objectId)` and full-batch validate-before-retain is covered by `dxmt9-chunk-record-registry-spec` and `WireObjectRegistry.tla`; builder dedup checks identity/pointer agreement. The write-only `g_wireObjectCache` is removed. `WireObjectRegistry::RetainFn` is statically `noexcept`; every registry method now fails callback re-entry closed before locking in debug and release, and the native test exercises that callback path without corrupting the outer retain. | Add local `(kind, pointer)` collision coverage and complete the remaining identity/model binding. Do not require atomic COM refcounts globally; `D3D9StateBlockImpl` is the preserved atomic exception. |
| Hot/cold and disabled observer boundary (`R-CORE-REC-5.*`) | ⚠️ owners and interface cleanup landed; broad codegen/wild evidence open | `PeRecorderState` retains hot protocol/shadow/counter ownership; a nullable heap-owned `PeCaptureState` retains capture correctness state; nullable `PeDiagnosticsState` owns the diagnostic-only accumulators, clocks, cadence atomics, maps, sampler, and child observer. Commit clocks/stats, UP counters, append phase clocks, TLS writes, and hot setter timers are nullable-gated. The four diagnostic virtuals are removed from `D3D9PeRecorderFlush`; child/device scopes use a concrete nullable observer, while protocol and Render Tape virtuals and `FlushPeRecorderForChild` key-function placement remain. `dxmt9-pe-diagnostics-spec` proves zero disabled allocation/callback/clock calls, one owner allocation for each individual gate (including Debug logging), enabled call/clock counts, diagnostic-field ownership, refcount policy, and source seams. Representative x64 object inspection confirms the sole device vtable remains in `d3d9_pe_device.cpp.obj`, while `SetRenderState` and `Surface::GetDesc` use nullable branches with direct enabled-only diagnostic calls. | Broaden PE instruction/code-size coverage across the remaining representative entry points and add Wine/wild diagnostics-on evidence. The native and representative object pins do not by themselves prove a runtime speedup. |
| Shared predicate/model-code binding (`R-CORE-REC-6.*`) | ✅ state-write/append algebra / ⚠️ commit-capture continuation | `d3d9_pe_transition_algebra.hpp` is host-buildable and production-used; `PeRecorderTransition.tla` uses the exact `PlanRecorderStateWrite` / `SettleRecorderAppend` names, checks 83,727 generated / 20,424 distinct states at depth 18, and its `ConsumeOnPrepare` cfg produces the expected `NoLostPending` violation. History witnesses prove recording explicit writes are record-only and prior-value writes preserve recorded/pending domains. `dxmt9-pe-transition-algebra-spec` exhausts the C++ truth tables; native, PE x64, and PE x86 builds are the acceptance lanes. | Post-effect Apply failure and the shared seal/bridge/capture-journal settlement transition remain open, as do wild/Wine runtime evidence; do not infer them from the append-local model. |

## Implementation order

Follow [spec.md §8](spec.md#8-sequential-implementation-dag): establish failure
injection first; clean observers/cache; repair append/commit settlement; then
decompose state/ownership, qualify identity, bind shared predicates, add formal
evidence, and finish with PE/Wine acceptance. These tracks are sequential
because they overlap the recorder hot files and because later proofs depend on
the earlier failure semantics.
