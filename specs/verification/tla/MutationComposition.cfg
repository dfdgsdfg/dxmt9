CONSTANTS
  Resources = {1, 2}
  Generations = {1, 2}
  TapeIdentities = {0, 1, 2}
  Ordinals = {1, 2, 3, 900}
  Offsets = {0, 4, 8, 16}
  Sizes = {4, 8, 16}
  Dispositions = {"plain", "nooverwrite", "discard"}
  Completions = {"complete", "pending"}
  Booleans = {TRUE, FALSE}
  Decisions = {"candidate", "different-resource", "different-generation",
               "render-tape-identity", "source-order", "barrier", "failure",
               "completion", "disposition", "range-overlap"}
INIT Init
NEXT Next
INVARIANT TypeOK
INVARIANT DecisionMatches
INVARIANT DecisionVocabulary
INVARIANT MixedPathBinding
INVARIANT ResetBinding
