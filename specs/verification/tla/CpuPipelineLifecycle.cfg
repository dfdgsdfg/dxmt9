CONSTANTS
  MaxSources = 3
  Capacity = 1
  RequiredParts = 2
  PresentSources = {2}
  ParallelSources = {1}
  SelectedSources = {1}
  InlineSources = {3}
  FailureSources = {}
  DirectSources = {1}
  AllowReset = FALSE
  AllowTeardown = TRUE
  WakeDiscipline = "Notify"
  ResetDiscipline = "Advance"
  InlineDiscipline = "Explicit"
  PresentDiscipline = "Enforced"
  OwnerDiscipline = "Exact"
  ReceiptDiscipline = "Exact"
  ImportDiscipline = "Exact"
  DeviceLossDiscipline = "Exact"

SPECIFICATION Spec
CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT OccupancyExact
INVARIANT GenerationQualified
INVARIANT PublicationComplete
INVARIANT JoinBeforeReceipt
INVARIANT CompletionPrefix
INVARIANT PresentSettledBeforeReclaim
INVARIANT NoGpuMilestone
INVARIANT WakeOnReclaim
INVARIANT ResetGenerationAdvances
INVARIANT OwnersAreExplicit
INVARIANT SelectedOwnerExact
INVARIANT ReceiptAuthorityExact
INVARIANT NormalCompletionState
INVARIANT ParallelJoinState
