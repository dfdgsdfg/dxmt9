\* TLC model checker configuration for WireObjectRegistry.tla.

SPECIFICATION Spec
CHECK_DEADLOCK FALSE

CONSTANTS
  Slots = {s1, s2}
  Requests = {r1, r2}
  Kinds = {Texture, Buffer}
  MAX_GENERATION = 3

INVARIANT TypeOK
INVARIANT NoZombieAccept
INVARIANT KindStable
INVARIANT NoReuseWithoutGenerationAdvance
INVARIANT NoGenerationWrap
