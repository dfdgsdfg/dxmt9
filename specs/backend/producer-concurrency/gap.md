---
type: "Gap Tracker"
title: "Producer Concurrency Gaps"
description: "Adoption and evidence gaps for the bridge synchronicity classification and thread-ownership contracts."
tags: [specs, backend, producer-concurrency, gap]
---

# Producer Concurrency — Gaps

Domain-owned tracker for `requirements.md` (R-BACK-43.x). Root rollup:
[../../gap.md](../../gap.md).

| Area | Status | Evidence / missing |
|---|---|---|
| Bridge entry classification table (R-BACK-43.1) | classification complete + mechanically audited (spec.md §3, per-entry ```classification block, 161 symbols — the 2026-08-21 "~89" total was an undercount, corrected in the same audit) | Mechanical drift audit landed: `scripts/check/audit_bridge_entry_classification.py` / `dxmt9-bridge-entry-classification-audit` diffs every `extern "C" dxmt9c_*` definition (five `device_c_bridge_*.cpp` files, plus a scan of the rest of `src/` for a stray direct-entry addition) against the spec.md §3 block and fails on any drift or out-of-taxonomy class. G1-G5 all resolved with file:line or measured evidence (G5: bridge_state=2 calls per full GT2 run — hot Set*/Draw* rides commit_chunk, the wire entries are cold fallbacks). The classification table is now fully grounded; remaining drift risk is covered by the mechanical audit. |
| `record-only` de-synchronization (R-BACK-43.2) | G2 resolved: no getter migration | The 2026-08-21 source audit found the 14 drained device-state getters cost zero on GT2 (nine dead on the steady-state PE-shadow path, five with load-bearing drains on their shadow-miss fallbacks), so the first candidate class yields nothing; no remaining known record-only candidate besides commit_chunk's already-offloaded lane. Reopen only if a profiler surfaces a drained entry with measurable frequency. |
| Ownership declarations at field level (R-BACK-43.4) | implemented for §2's audited set + mechanically audited | Every row in §2 now carries an adjacent code declaration naming its class, indexed by the second table in §2: rename ring + capture read-set (`BufferRecord` / `struct Pool` / pool header contract), PE recorder + chunk builder + retainer, `nextSeqId_` (publication mechanism named), `completedSeqId_` (`owner-published` since T2c, with its own memory-order argument at the declaration), writing slot, low-4GB shadow pool. Mechanical drift audit landed: `scripts/check/audit_thread_ownership_declarations.py` / `dxmt9-thread-ownership-audit`, the natural pair with the R-BACK-43.1 audit above. It checks both directions — every `ThreadOwnershipToken` member declaration anywhere under `src/` must carry one of the six closed R-BACK-43.4 class names in a comment within a bounded window above the declaration (so a NEW multi-actor field cannot ship unclassified), and every declaration-site file named in §2's second table must still exist and still contain a `ThreadOwnershipToken` declaration or class-name comment (so a rename/deletion cannot leave §2 stale). It is regex-level like the R-BACK-43.1 audit, not a C++ parser, and it can only verify the two classes with a concrete `ThreadOwnershipToken` mechanism (`producer-owned`, `worker-owned`); the other four classes (`owner-published`, `arena-protected`, `queue-shared`, `immutable-after-init`) remain documentation-only per R-BACK-43.4 and are not mechanically checked against production behavior — only that a class name is present in the comment. |
| Shared thread-affinity assert helper (R-BACK-43.5) | implemented | `include/dxmt9/thread_ownership.hpp` — `dxmt9::core::ThreadOwnershipToken` (fixed-at-construction, rebindable, and unowned states) plus `DXMT_ASSERT_OWNED_BY` / `DXMT_ASSERT_OWNED_BY_OR_LOCKED`, empty and non-evaluating under `NDEBUG`. One header serves PE and unix: thread identity is the address of a function-local `thread_local`, so it needs neither `<thread>` nor `<windows.h>`. `assertRecorderThreadConfined` is migrated onto it and is no longer a private copy; new adopters are `Pool::producerOwnership_` and `CommandQueue::writingSlotOwnership_`. Missing: (a) the two asserts have UNEQUAL coverage — an inverted-assert negative control over the full suite aborts 17 tests through `Pool::producerOwnership_` (`dxmt9-dynamic-rename-ring-spec`, `dxmt9-render-tape-provider-spec`, and the `vs_specific` shader-corpus readbacks) but **zero** through `CommandQueue::writingSlotOwnership_`: no native test reaches `submitDrawRunBatchImpl`'s append, because the only spec that calls `submitDrawRunBatch` (`dxmt9-cpu-ready-arena-lease-spec`) short-circuits into the active-arena path first. That assert is therefore documentation of the T2d hazard, not a live check, until either a native spec drives the non-arena batch lane or T2d lands. Resolved since: (b) T2b's capture move was the outstanding *user* of the helper and has landed — `Pool::captureChunkBufferBindings` is a third `producerOwnership_` site, covered by the same 17-test negative control as the other three (it runs on every commit that names a buffer); (c) the static side of the drift risk is now mechanically audited — `scripts/check/audit_thread_ownership_declarations.py` / `dxmt9-thread-ownership-audit` (see the R-BACK-43.4 row above) fails if a `ThreadOwnershipToken` adopter ships without an adjacent class comment, so a fourth site could not silently join the three above unclassified. That is a static-declaration check only and does not touch (a): it cannot tell whether an assert actually *fires* in any test, so the writing-slot coverage gap stands exactly as described until a native spec drives the non-arena batch lane or T2d lands. |
| T2b capture off the queue mutex | implemented | `Pool::captureChunkBufferBindings` owns the loop and the `DXMT_ASSERT_OWNED_BY(producerOwnership_)` (the assert lives with the token, and the per-record `captureChunkBufferBinding` stays assert-free because `snapshotBufferBinding` reaches it from the worker's draw-run snapshot path). `CommandQueue::markChunkResourcesAndCaptureBufferBindings` keeps ONE acquire around the frozen-ticket re-read and nothing else; `CommandQueue::captureChunkBufferBindings` (CpuReadyTape planning lane) has no acquire at all. Model: `CaptureRead` already interleaved freely — the review confirmed it is guarded only by commit phase and per-record bookkeeping, never by a mutex-implying phase — so the extension is two invariants (`NoCaptureAfterFree`, `StampsPrecedeCapture`) plus `ProducerMarkReclaim.capture.counterexample.cfg`, which reuses the EXISTING `PinDiscipline="Removed"` axis rather than adding a redundant one and reports the capture-side violation in 6 steps. Production cfg unchanged at 1,140,594 generated / 276,840 distinct / depth 29. Native: `dxmt9-producer-mark-reclaim-spec` traces I-L (unrelated-reclaim interleaving, no-restamp-analog, stamps-before-capture enabledness, the translated capture counterexample). Pool header carve-out rewritten from "does not permit capture" to the narrower exception (2). **Evidence still owed: the profile re-measurement and the wild matched pair.** |
| T2c map DISCARD fast path (`completedSeqId_` → owner-published atomic) | implemented | `completedSeqId_` is `std::atomic<u64>` with the sole writer (`QueueLifecycleController::drainCompletedSequence`) doing a release store still under `mutex_`; `SubmissionBinding::completedSeqId` follows `nextSeqId`'s precedent as `std::atomic<u64>*`; locked readers use `completedSeqIdLocked()` (relaxed, exact under the mutex), the fast path uses `completedSeqIdAcquire()`. `mapBuffer` splits: `mapWaitSeqId == 0` runs `finalizeBufferMap` with NO queue-mutex acquire; the slow lane re-reads `mapWaitSeqId` under the lock and is otherwise byte-for-byte today's. Read-set classification for `mapWaitSeqId` recorded in spec §2 and at the call site — two `producer-owned` flavor flags plus the `arena-protected` `lastUsedSeqId`, all under HandleArena's shared lock, and no `queue-shared` operand, so the unlocked read's race profile is identical to the locked one (T2a' already took that watermark off `mutex_`). **Evidence still owed: the profile re-measurement and the wild matched pair; burst-structure check per the .31 rule before any fps expectation.** |
| T2d reserve-copy-commit slot append | design stated (design doc §9) | Needs its own bounded model (append/publish/force-publish interleaving with a half-appended-slot Buggy cfg) before implementation. Largest remaining holder (3.4-3.8 ms/present). |
| C++ memory-order harness (R-BACK-43.6 residual) | implemented, with the scope statement below | `tests/native/backend/producer_interleaving_spec.cpp` + `interleaving_scheduler.hpp` (`dxmt9-producer-interleaving-spec`) — design brief §5 layer 3, the layer `producer_mark_reclaim_spec.cpp`'s own closing paragraph defers to. Real threads on a scripted cursor drive **real production symbols**: the `ArenaLeaseTestQueueTag` `CommandQueue`, its `nextSeqId_` atomic and `mutex_`, `markTicketAcquire()`, `markChunkResources` / `markChunkResourcesAndCaptureBufferBindings` (so `seqIdForMark` and `restampIfTicketAdvancedLocked` execute for real), and `Pool::markBufferUse` / `markBufferDestroyAndGc` / `reclaimCompleted` / `captureChunkBufferBinding` over the real `HandleArena` `shared_mutex` + `deque`. **Mirrored**, each citing its production line and pinned by an in-spec source-text audit that fails on drift: the `nextSeqId_` publisher (`dxmt9_queue.cpp:1734`, `dxmt9_command_queue.cpp:3883`), the four-line `restampIfTicketAdvancedLocked` body (`dxmt9_command_queue.cpp:2255` — decomposed only so schedule S2A can place a race-free breadcrumb inside the window; S2B runs the same race through the integrated call), and the PE retainer pin (`d3d9_pe_retainer.hpp`, not linkable unix-side). Schedules: both TLC counterexample traces translated (`PinDiscipline`, `RestampDiscipline`), each asserting the safe outcome on production code AND asserting the buggy world still reproduces the fault; window edges either side of the ticket read; `MarkMonotonic` across two actors; reclaim racing another record's stamp; capture during an unrelated record's erase (also pins R-VERIF-3.4 deque pointer stability); and 384 fixed-seed schedules checking invariants after every step with a same-seed digest gate. **Scope — four honest limits.** (a) The scripted cursor is itself a release/acquire edge, so scripted schedules prove *protocol* order and are structurally blind to *memory* order. (b) The spec records a finding that bounds the whole row: today's mark path does **not** depend on the release/acquire pair — `dxmt9_command_queue.hpp:886` already says "The mark path does not depend on that" — because the protocol needs only atomicity, monotonicity, and a frozen re-read ordered by `mutex_`. The pair is the guarantee a lock-free consumer of the *published slot* would rest on, i.e. exactly what T2b/T2c/T2d each add; the free-running lane stands in for that consumer so the guarantee is pinned before something depends on it. (c) The seeded lane is sync-only by design — an async step would make the outcome timing-dependent and forfeit "same seed ⇒ same result" — so it explores step-level, not intra-call, interleaving; intra-call is covered by S2 and the two free-running lanes. (d) `submitDrawRunBatchImpl`'s worker-side T2a' mark is still unreachable from a native spec, for the same reason the R-BACK-43.5 row above records; the shared helper is covered, that call site's surroundings are not. **TSan lane:** `meson setup build-tsan -Db_sanitize=thread -Db_lundef=false`, then run the binary with `TSAN_OPTIONS=halt_on_error=1:abort_on_error=1` — clean 2026-08-20 on arm64, and TSan was confirmed armed by a positive control. Note `tests/meson.build` sets halt-on-error for ASan/UBSan/MSan but **not** TSan, so a plain `meson test -C build-tsan` prints a race warning and still reports OK; cite the explicit invocation, not the meson line. **Measured detection power** (this is why the lanes are not redundant, and why only one of them is a real gate): the deliberately-`relaxed` negative control read `19,012 / 27,291 / 2 / 0 / 147` violations per 200k–400k observations across five plain arm64 runs, against `0` every time for the production release/acquire pair — and `0` under TSan, whose instrumentation serializes the window. The low readings came from runs with two other build trees saturating the machine. So a nonzero count proves the lane can see a weakened order, a zero count proves nothing, and the free-running lane and the TSan lane mask each other. Neither is a gate. On x86-TSO the free lane is blind by construction. The deterministic guard — the one that fails on every host under any load the moment an order is weakened — is the in-spec source-text audit, which carries its own negative control that applies the exact weakening to the real production text and requires the matcher to reject it. |
| Constant-promotion complexity review (R-BACK-43.7) | process rule active from this spec; known open instance closed | Two recorded recurrences motivated it; enforcement is review-time. The commit dedup instance (O(n²) at 277 handles/call, ~0.17 ms/present, `state-churn-encode-append-decomposition.{26,28}`) is closed: `persistResolvedResourcesAndCaptureBindings`'s `raw.ledgerTargets`/`scratch.coreEntries` dedups (`device_c_chunk_replay.cpp`), `D3D9PePendingCommandRetainer::retain` (`d3d9_pe_retainer.hpp`), and `CommandChunkBuilder::referencesObject` (`d3d9_pe_chunk_builder.{hpp,cpp}`) now resolve duplicates through open-addressed (kind/pointer) -> index or presence tables instead of a linear scan, each falling back to the original O(n) scan only past a measured capacity (chunk-scoped presence tables) or growing amortized (the retainer's chunk-lifetime index). Index-consistency pins (`dxmt9-pe-retainer-spec`, `dxmt9-pe-chunk-record-value-spec`) cover retain-after-rollback, endEpoch compaction rebuild, growth, and overflow fallback. `appendHandle`'s own record-local dedup scan was left unchanged — its window is bounded to tens of handles per record, below the size where this rule bites. |
| Restamp-fire observability | implemented | `mark_ticket_restamp_checks` / `mark_ticket_restamp_fires`, incremented once per `restampIfTicketAdvancedLocked` via `perf::countMarkTicketRestamp(bool)`. `fires/checks` is how often a concurrent publish really lands inside a lock-free mark window. Always on: two `enabled()`-gated atomic adds on a path that already holds `mutex_`. Schema golden regenerated; all four perf-counter audits green. No wild reading taken yet — that arrives with the T2b/T2c profile re-measurement. |
| Producer commit acquire not fully removed | open | T2b emptied the producer's commit-path critical section but did not delete the acquire: `seqIdForMark` READS `nextSeqId_` where `ProducerMarkReclaim!BeginMark` RESERVES it (`nextSeqId' = nextSeqId + 1`), so the model's producer premise — "commit_chunk owns the seq its chunk publishes under", which is what makes `CommitStampsCoverChunkSeq` hold without a re-stamp — is stronger than production. The re-stamp is therefore insurance rather than a proven no-op, and insurance needs the frozen ticket, which needs the mutex. Closing this means either making the producer's ticket a real reservation (and re-checking the model with `BeginMark` matched to it) or proving the current read sufficient; only then does the producer's LAST commit-path acquire go. Cheap to observe first: `mark_ticket_restamp_fires` on the producer sites. |
