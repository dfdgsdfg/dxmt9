---
type: gap
title: Managed Buffer Mutation Offload — Gap
description: Implementation and evidence status for the default-on R-BACK-44.x transport and planned composition observer.
tags: [backend, buffers, producer-concurrency, offload]
---

# Managed Buffer Mutation Offload — Gap

Status of R-BACK-44.1..44.11. The formal stack, production implementation, and
wild promotion evidence landed 2026-08-25; V1 is default on with an explicit
mode-off rollback lane. The observation-only composition ledger is now present,
but composition remains forbidden until representative runtime evidence and
the decision gate pass. Sizing evidence:
`docs/perfomance/present-pacing/present-pacing-bridge-crossing-decomposition.237.md`
(Managed re-uploads `1.19ms/present` on the saturated GT2 producer thread;
`.38`'s `>=0.5ms/Present` mutation-stream design gate is met).

A 2026-08-25 external design review (codex `gpt-5.6-sol`/high; findings
re-verified against source) returned "implement with changes": four
blockers resolved by design amendments (V1 scope narrowed to plain Managed
writable locks; reserve/rotate/commit admission transaction; encode-side
snapshot-sourcing promoted to a prerequisite; task backing lease) and five
majors folded into the requirements. Rows below reflect the amended
design.

| Area | Status | Evidence / missing |
|---|---|---|
| **Mutation composition observer and decision gate (R-BACK-44.9–44.11)** | bounded production observer implemented; prior economic result retracted as ordinal-domain-invalid; composition forbidden | `D9CDevice` owns an optional fixed-capacity observer behind `DXMT9_MUTATION_COMPOSITION_OBSERVER`. Successful synchronous and deferred writable Unlocks publish exact resource handle, backing revision, disposition, range, bytes, source ordinal, timing split, and completion disposition; the replay worker publishes the captured backing's first GPU use and direct readonly Lock publishes the first CPU observer. The observer now assigns every mutation/use a typed, generation-qualified ordering identity at ingress, while retaining the production ordinal for exact settlement. Reset/reentry advances the ordering generation and fails closed on exhaustion; mixed sync↔deferred adjacency is covered by native and TLA witnesses. The earlier matched GT2 observer-off/on bundle remains valid as a behavior-neutral run, but its zero-candidate claim is unreliable because synchronous observations used a private counter while deferred/replay observations used `replaySeq`; it cannot close the `<0.2 ms/Present` gate. The disabled path constructs no observer and current V1 FIFO task-per-Unlock transport remains unchanged; fusion is not implemented. See [`present-pacing-mutation-composition-observer.238`](../../docs/perfomance/present-pacing/present-pacing-mutation-composition-observer.238.md). |
| **PREREQUISITE: encode-side versioned byte readers snapshot-sourced (R-BACK-44.4a)** | implemented (2026-08-25, `8f6d7d4e` + `d5cccd40`); runtime debt closed | A same-day external implementation review (codex `gpt-5.6-sol`/high, verdict "unsound", claims re-verified) found the guard idiom insufficient: `snapshotBufferBytes()` returns an empty span for a valid snapshot with `contentsAddress == 0` (reachable via shared-buffer import), and every snapshot-first site branched on span emptiness, falling through to live reads — including five pre-`8f6d7d4e` sites. Follow-up rewrites every site to branch on snapshot presence (bytes-unavailable skips the byte-dependent path), converts racy live-field trace logging to snapshot fields, and adds regression tests for the zero-contents snapshot and staging suppression. |
| PREREQUISITE landed base (`8f6d7d4e`) | context for the row above | `8f6d7d4e` added the `!indexSnapshot` staging guard mirroring the vertex path, snapshot-first sourcing for the two trace-gated diagnostic index readers, and the `StreamIbStagingCache::findOrStage` no-versioned-record assert (`dxmt9_draw_encoder_draw.mm`, `dxmt9_encode_session_storage_internal.hpp`); 19 encoder/hazard/session native specs pass. The pre-existing-race framing stands: the R-BACK-2.51(d) drain waits on `lastReplayedSeq` only and never covered encode-time reads. Runtime evidence: the first GT2 smoke ran concurrently with an unrelated heavy host workload and was discarded; a separate clean-machine run (2026-08-25, user-supervised) behaved normally with the FPS delta inside noise, closing the runtime debt for the prerequisite. |
| Queue reservation API (reserve/commit/release) in `ReplayOffloadQueue` | implemented (2026-08-25) | Placeholder-at-tail reservation: `reserveMutation` appends the queue slot and takes the ledger ordinal in one queue-then-ledger critical section, `commitMutation` is an infallible pointer move, `releaseMutation`/`drainRemaining` refund budget on every reject/stop/teardown path; `pop()` blocks while the head is an unfilled placeholder so the worker cannot bypass an uncommitted reservation. Staged bytes charge the `DXMT9_OFFLOAD_QUEUE_BYTES` bounds. Pinned by `dxmt9-managed-mutation-offload-transaction-spec` (reservation ordering vs interleaved chunk push, release refund, budget block/admit, stop/poison pre-effect rejection). |
| Task backing lease (R-BACK-44.2a) | implemented (2026-08-25) | `dxmt9_managed_mutation_lease.hpp` (`ManagedBufferMutationLease`: renameIndex, metalHandle, contents, contentRevision, byteSize, backingResidency); record half taken at reserve (core-buffer retention + drain-target ownership), ring-entry half returned by the synchronous rotation; the leased entry holds replay residency so the next rotation cannot reuse it, and apply re-checks index/handle/contents and fail-stops on mismatch. Pinned by `testHeldLeaseKeepsTheNextRotationOffTheSameEntry` and the lease-mismatch rejection case. |
| R-BACK-2.51(d) amendment (fourth admission form (iv)) | implemented (2026-08-25) | Clause (iv) added to `specs/backend/requirements.md` R-BACK-2.51(d) in the same change as the mode implementation. |
| Formal model `BufferMutationOffload.tla` + production cfg (R-BACK-44.3/44.4) | implemented (2026-08-25) | `specs/verification/tla/BufferMutationOffload.tla`: `EncodeReadsAppliedBytes` (encode of chunk `k` observes every mutation `< k` applied; content modeled as revision identity), `SnapshotRevisionIsCurrent` (capture reads the revision the app has been acked), `NoLiveCaptureOverwritten` (rotation never targets a backing captured by a committed-but-not-encoded chunk — deliberately weaker than the production `completedSeqId` gate so production behaviors are a subset), `FifoApplicationOrder`, ordinal uniqueness, revision conservation, and the R-BACK-44.2a lease shape. TLC production run: no error, `65,442` states. Composes with `BufferBackingVersioning.tla` without weakening `NoUploadOverwriteInFlight`/`NoBackingFreedInFlight`. |
| Counterexample configs (R-BACK-44.6 via R-BACK-43.6) | implemented (2026-08-25) | `.counterexample.cfg` (second-queue/out-of-order apply) fails `EncodeReadsAppliedBytes`; `.rotation.counterexample.cfg` (rotation deferred to apply) fails `SnapshotRevisionIsCurrent`. The two failures are independent: a deferred rotation yields a coherent stale read that invariant 1 cannot see. Both wired into `verify_tla.sh` `counterexample_models` with exact expected messages; full script exit 0. |
| Shared pure predicates + native spec | implemented (2026-08-25) | `src/dxmt9/dxmt9_mutation_offload_predicates.hpp` (admission incl. plain-writable lock class, FIFO ordering operators, capture-freshness; `directReaderMustFence` bound to `ReplayScopedDrain`, which owns R-BACK-44.5, not to this model) + `dxmt9-buffer-mutation-offload-spec` (truth tables + both TLC traces as trace-conformance cases). |
| FIFO task variant in `ReplayOffloadQueue` | implemented (2026-08-25) | `ReplayQueueItem` (chunk / mutation / placeholder) with worker per-alternative dispatch under the same fail-stop discipline; `chunk -> mutation -> chunk` FIFO order pinned by `dxmt9-managed-mutation-offload-transaction-spec`; teardown `drainRemaining` discards pending tasks in order, releasing retention and byte budget. |
| Synchronous-half split in `Buffer::unlock` / `Pool` | implemented (2026-08-25) | `Pool::rotateManagedBufferForMutation` (rotation + revision bump without the byte copies), `Pool::applyManagedBufferMutation` (worker-side copy-forward + patch to the leased entry, byte-identical to the synchronous upload — pinned by `testDeferredApplyMatchesSynchronousUploadByteForByte`), the `dxmt9c_buffer_unlock` transaction branch with all-layer lock-state clearing deferred past commit and retryable pre-effect rejection (PE `unlockPeBuffer` verified failure-tolerant, no PE change needed), ledger publication via `publishMutationAcceptedLocked` (max-merge). Admission evaluated once at the bridge fence (`admitsManagedMutationOffload` from the shared predicates) and carried via `mutationOffloadPlanned`. Mode-gated Managed-`NOOVERWRITE` bypass fix included (see R-BACK-44.5). Counters: `d3d9_buffer_unlock_deferred_*`, `offload_buffer_mutation_*`. |
| Bridge synchronicity subpath (`dxmt9c_buffer_unlock`) | implemented (2026-08-25) | Entry keeps its `visibility-wait` class (no table change); the mode-conditional non-waiting subpath is documented as an ordering protocol in `specs/backend/producer-concurrency/spec.md` §4 with the `BufferMutationOffload` model binding; `dxmt9-bridge-entry-classification-audit` green. |
| Direct-reader fence coverage (R-BACK-44.5) | implemented (2026-08-25) | Mutation tasks publish `lastQueuedSeq` (max-merge) and the worker bumps `lastReplayedSeq` after apply, so existing (d)(i) fences cover pending mutations with no reader-side change; shared-buffer export keeps its conservative global drain. The Managed-`NOOVERWRITE` unconditional bypass is now conditional on no pending target work in offload mode (a full synchronous upload would race a queued mutation); mode-off byte-identical. |
| Mechanism counters | implemented (2026-08-25) | `d3d9_buffer_unlock_deferred_{calls,staged_bytes,stage_ms,rotate_ms,rejected_calls}` (producer) and `offload_buffer_mutation_{applied,apply_cpu_ms,apply_cpu_max_ms,copy_forward_bytes,patch_bytes,discarded}` (worker); schema golden regenerated, all four counter audits green. |
| Wild promotion evidence (R-BACK-44.8) | implemented (2026-08-25); engine default flipped ON | GT2 matched A/B (`mutation-offload-off-gt2-r1` vs `mutation-offload-on-gt2-r2`, same build): harmonic scene fps `27.605 -> 28.864` (`+4.6%`), p50 `32.81 -> 31.92ms`, p95 `44.41 -> 43.18ms`, mechanism exact (`1,228` deferred = `1,228` applied, zero rejects/discards), producer unlock CPU `3,295 -> 1,390ms/run`, managed full uploads `1,227 -> 0`, drain-fence wait at baseline (`208ms`), zero GPU errors. The first ON run (`-r1`, `-2.6%`) exposed an over-broad NOOVERWRITE bypass probe (fence wait `+2.30ms/present` at `dxmt9c_buffer_unlock`, drain-site attributed) fixed by scoping the probe to the Managed pool (`ed8690f3`). GT1 visual: pass, `441` applied, normal frame, zero errors. Completing gates (2026-08-25): GT3 visual pass (`354` applied, zero errors), SFIV pass (`717` applied, zero errors, normal `76s` benchmark), locality conserved (`render_pass_begin/present` `15.801 -> 15.790`, `sub_command_buffers/present` `2.998 -> 2.998`, one CB sample/present both), and the D3D9 conformance suite showing zero mode-caused delta: mode ON and OFF fail-identically at HEAD (`254` tests, `235` pass, the same `11` failures both ways — all device-creation/WSI/present-policy cases owned by the concurrent wine-wsi-interop merge, none buffer-related; root-caused same day to the conformance runner not declaring the harness-owned WSI identity pair — fixed in `cb7922c2`, suite `235 -> 248` pass. The earlier '2026-08-24 baseline' citation was wrong: that archive is a single-test run; the correct pre-WSI full baseline is `tmp/d3d9-conformance-prehoist.json` (2026-08-02, `247` pass / `1` long-standing fail), which also has none of the 11). Engine default flipped ON; explicit `0` remains the byte-identical rollback lane. |
